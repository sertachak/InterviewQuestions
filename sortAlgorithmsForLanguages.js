/* Sort algorithm for languages:
Javascript => according to ES it can be differ by which program it is used. 
E.g. Mozilla uses merge sort because of V8 engine. However Safari and Chrome has their own engine so they can be change it, 
currently they are using quick sort for big data and insertion sort for small data.

C++ -> C++ has sort() and qsort() algorithm. Qsort is a quicksort algorithm it takes comparator, 
element array and pointer to that array. Sort algorihm is a hybrid algorithm which is introSort.
 It is a hybrid of quicksort and heapsort which is followed by a insertion sort on the result.

 */